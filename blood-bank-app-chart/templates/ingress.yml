apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bankapp-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bloodbank-service
                port:
                  number: 80

